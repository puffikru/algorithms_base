#include "../src/diamonds.h"

// #include "../4.7.7/googletest/googletest/include/gtest/gtest.h"

#include <fstream>
/*
TEST(BasicTests, Test) {
    //Arrange
    int k = 2;
    std::vector<std::pair<int, int>> diamonds = {{6792, 19949}, {22986, 3872},
        {28903, 6506}, {2514, 1990}, {23158, 5029}, {2101, 23700}};

    //Act
    int res = CountBest(diamonds, k);
    int x = BinarySearch(diamonds, res, k);

    //Assert
    ASSERT_EQ(x, res);
}

TEST(BasicTests, SimpleTest) {
    // Arrange
    int k = 2;
    std::vector<std::pair<int, int>> diamonds = {{1, 1}, {1, 2}, {1, 3}};

    // Act
    int res = CountBest(diamonds, k);
    int x = BinarySearch(diamonds, res, k);

    //Assert
    ASSERT_EQ(x, res);
}


TEST(BasicTests, Test_1) {
    int k = 3;
    std::vector<std::pair<int, int>> diamonds = {{10, 1}, {10, 1}, {9000, 1000}, {8, 1}};

    double res = CountBest(diamonds, k);
    double x = BinarySearch(diamonds, res, k);

    ASSERT_EQ(x, 9.33333);
}


TEST(BasicTests, Test_2) {
    int k = 1;
    std::vector<std::pair<int, int>> diamonds = {{1, 3}, {1, 2}, {1, 1}};

    double res = CountBest(diamonds, k);
    double x = BinarySearch(diamonds, res, k);

    ASSERT_EQ(x, 1.00000);
}

TEST(BasicTests, Test_3) {
    int k = 3;
    std::vector<std::pair<int, int>> diamonds = {{1, 3}, {1, 2}, {1, 1}};

    double res = CountBest(diamonds, k);
    double x = BinarySearch(diamonds, res, k);

    ASSERT_EQ(x, 0.50000);
}


TEST(BasicTests, Test_4) {
    int k = 1;
    std::vector<std::pair<int, int>> diamonds = {{1, 1}};

    double res = CountBest(diamonds, k);
    double x = BinarySearch(diamonds, res, k);

    ASSERT_EQ(x, 1.00000);
}



TEST(BasicTests, Test_5) {
    int k = 1;
    std::vector<std::pair<int, int>> diamonds = {{22769, 19854},
        {13278, 10099}, {13332, 2295}, {21869, 10033}, {4844, 17713}};

    double res = CountBest(diamonds, k);
    double x = BinarySearch(diamonds, res, k);

    ASSERT_EQ(x, 1.00000);
}
*/
